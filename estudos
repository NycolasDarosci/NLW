<label for="name"></label>
<input id="name">
<!-- input - entrada de dados   
label pertence ao input -->

fieldset : formulario

OUTLINE: ao clicar, mostra uma borda, serve para 
        acessibilidade

BOTÃO DENTRO DE UM FORMULÁRIO (form)
- o botão por padrão, quando estiver dentro de
um formulario (form), ele irá querer enviar o
formulario (form), e quando eu não quiser enviar,
eu tenho que colocar um (type ="button")

PLACEHOLDER 
- uma mensagem que, ao digitar, ele some 


map
- é um objeto, de acordo com esse projeto

map.on('click', function)
- quando eu clicar no mapa, ele vai executar uma funcionalidade

let marker;
- let serve para que eu altere 'marker' a qualquer momento

 marker && map.removeLayer(marker)
 - na segunda vez que eu clicar, 
   existe o marker, então (&&) irei remover a Layer marker do mapa

document.querySelector('[name=lat]')
-'querySelector('[name=lat]'), 
 procurar pelo seletor ('[name=lat]') dentro do html.        
 
span.parentNode.children[0].value = ""
- peguei o "new-upload" (parentNode) do span, que está na posicão 0,
        e coloquei o valor igual a "vazio"

document.querySelectorAll('.button-select button')
    .forEach(function(button){
        button.classList.remove('active')
    })

-selecionar os ('.button-select button'),
e para cada, terá uma função que será realizada no 'button',
a classe de lista do botão, removerá o 'active'



--------------------BACK-END-----------------------------

Live Server é um servidor que manda a resposta através do HTML,CSS,JS
o BACK-END serve como um servidor, iremos criá-lo


RESPONSABILIDADES

    -receber pedidos do cliente 
    -Preparar os pedidos (!! REGRAS DE NÉGOCIOS !!) <-- importante
    -Enviar respostas  
    -Lidar com dados de aplicação

TECNOLOGIAS

    -Linguagem: Javascript
    -Node.js - responsável por rodar js na máquina
    -Git Bash - novo TERMINAL (como o cmd prompt, power shell, oferecidos pelo windows)
                responsável por verificar se o node está instalado
                e rodar arquivos js através dele

                BACKEND
npm - node package manager

npm init -y
- iniciar um package.json

../ - caminho relativo  } TEM HAVER COM AS PASTAS
/ - absoluto            } TEM HAVER COM AS PASTAS          


package.json
- java script object notation

127.0.0.1:5500 está funcionando e eu tenho uma rota '/'